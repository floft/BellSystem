Possible:
	* warn when multiple schedules for same day
	* convert times on each file change to unix time???

0.2:
	* password in .password file (created by ./password.sh)
	* redesigned XML code
	* rewrote daemon in C++
	* rewrote website ui
	* added reverse SSH tunnel daemons
	* the daemon is a daemon, not run from cron
	* only reload config when last modified changed
0.1:
	* initial run
